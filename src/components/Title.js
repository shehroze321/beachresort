import React from 'react'

const Title = ({title}) => {
  return (
    <div>
      <div className="section-title">
        <h4>{title}</h4>
        <div/>
      </div>
    </div>
  )
}

export default Title;
